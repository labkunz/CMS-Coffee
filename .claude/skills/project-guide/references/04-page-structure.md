# 04 — 頁面結構 & 路由規劃

> 📌 前置條件：先讀完 `02-content-model.md` 了解資料結構

開始寫 code 前先確認整體架構。

---

## Nuxt 4 目錄結構

Nuxt 4 使用 `app/` 目錄結構：

```
app/
├── pages/
│   ├── index.vue              # 首頁（Hero Banner + 精選品項 + 店家簡介）
│   ├── menu/
│   │   └── index.vue          # 菜單頁（所有品項列表 + 分類篩選）
│   ├── menu/[slug].vue        # 單品詳情頁（動態路由）
│   └── about.vue              # 關於我們
├── components/
│   ├── HeroBanner.vue         # 首頁 Banner
│   ├── MenuCard.vue           # 品項卡片（共用元件）
│   └── CategoryFilter.vue     # 分類篩選
├── composables/
│   └── useContentful.ts       # Contentful API 串接邏輯
├── types/
│   └── contentful.ts          # TypeScript 型別定義
└── app.config.ts              # Nuxt UI 主題設定
```

---

## 各頁面的技術展示目的

| 頁面 | 路由 | 展示的技術重點 |
|------|------|----------------|
| 首頁 | `/` | CMS 資料串接、元件化設計、SSR |
| 菜單列表 | `/menu` | API 資料列表渲染、分類篩選邏輯、computed 響應式更新 |
| 單品詳情 | `/menu/[slug]` | 動態路由、單筆資料查詢、404 錯誤處理 |
| 關於我們 | `/about` | Rich Text 渲染、CMS 管理靜態頁面內容 |

---

## 元件設計原則

### `MenuCard.vue`（共用元件）

這個元件會在首頁和菜單頁重複使用。接收的 props：

- 品項名稱
- 價格
- 分類標籤
- 圖片 URL
- slug（用於產生連結）

### `HeroBanner.vue`

首頁專用，接收從 `homepage` Content Type 取得的資料。

### `CategoryFilter.vue`

菜單頁專用，用 Nuxt UI 的 Tabs 或 ButtonGroup 元件，觸發分類篩選。
